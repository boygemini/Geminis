{
	"version": 2,
	"builds": [{ "src": "server.js", "use": "@vercel/node" }],
	"routes": [
		{
			"src": "/client/(.*)",
			"dest": "/client/$1"
		},
		{
			"src": "/(.*)",
			"dest": "/server.js"
		}
	]
}
